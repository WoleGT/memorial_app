name: Deploy Laravel Project on push

on:
  push:
    branches:
      - main

jobs:
  web-deploy:
    name: Deploy
    runs-on: ubuntu-latest

    steps:
      - name: Get the latest code
        uses: actions/checkout@v3

      - name: Set up PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'

      - name: Install Dependencies
        run: composer install --no-dev --prefer-dist --ignore-platform-reqs

      - name: Cache Config & Views
        run: |
          php artisan config:cache
          php artisan view:cache

      - name: Directory Permissions
        run: chmod -R 777 storage bootstrap/cache
        
     

      ## Deploy to Staging
      - name: 📂 Deploy to MyAfrimall API
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server:  ftp.oluwolegeorgetaylorsnrmemorial.online
          username:  memorial@oluwolegeorgetaylorsnrmemorial.online
          password: OluwoleMemorial2025!@
        
      # - name: 🔄 Optimize Application
      #   run: php artisan cache:clear
